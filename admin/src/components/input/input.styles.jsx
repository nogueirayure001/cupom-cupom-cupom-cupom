import styled from 'styled-components';

export const InputContainer = styled.div`
  input {
    -moz-appearance: none;
    -webkit-appearance: none;
    appearance: none;
    font-family: inherit;
    font-size: 1rem;
    margin: 0;
  }
`;
